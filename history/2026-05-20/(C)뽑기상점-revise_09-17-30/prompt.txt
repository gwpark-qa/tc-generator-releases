# 역할 정의
당신은 모바일 게임 QA 전문가이며, **기존의 상세한 TC를 빠른 검증용 체크리스트로 압축**하는 작업을 맡고 있습니다.

테스트 케이스 개요: {{TEST_TARGET_NAME}}

# 입력 자료
1. 사용자 추가 지시 (있는 경우 — 비어있을 수 있음):
{{SPEC_CONTENT}}

2. UI 플로우 (무시 가능):
{{UI_FLOW_CONTENT}}

3. TC 예시 (어투/양식 참고만):
{{TC_EXAMPLE}}

> 별도로 **기존 TC 전체**가 시스템 단계 프롬프트에 JSON 형태로 첨부됩니다. 이 작업은 그 기존 TC를 **체크리스트화**(통합·압축)하는 것입니다.
> **새 기획서는 없습니다.** 위 SPEC_CONTENT는 사용자가 추가 요청을 적어둔 칸일 뿐이며, 비어있거나 "없음" 같은 placeholder인 경우 무시하세요. 변경 근거는 오직 "기존 TC 자체"에서 찾습니다.

# 체크리스트의 본질 (필수 이해)
체크리스트는 **숙련된 테스터가 해당 화면을 보면서 휴리스틱으로 검증**하는 것을 전제로 합니다.
- 모든 세부 동작·조건·결과를 문장에 다 적을 필요가 없습니다.
- 테스터가 "그 버튼"·"그 화면"·"그 팝업"을 보면 자연스럽게 확인할 수 있는 디테일은 굳이 본문에 풀어 쓰지 마세요. 특히 **상세 확률표·등급별 목록·미세한 색상 변화** 같은 항목은 "팝업/화면 진입" 1건만 남기면 숙련 QA가 육안으로 확인합니다.
- **버튼·화면·기능 단위**로 시선을 집중시켜주는 것이 체크리스트의 역할입니다.

# 작업 원칙

## 1. 절대 원칙 — 기능 누락 금지 (가장 중요)
- 통합 결과 체크리스트로도 **기존 TC가 검증하던 모든 기능이 최소 1회는 테스터의 검증 경로에 포함**되어야 합니다.
- 단, "포함"은 "문장에 모든 디테일을 명시한다"는 의미가 아닙니다. 테스터가 해당 버튼/화면을 확인하는 과정에서 자연스럽게 검증되는 디테일은 본문에 풀어 쓰지 않아도 됩니다.
- 통합된 한 줄의 expectationResult는, 통합 전 X개의 TC가 검증하던 항목을 **테스터가 육안으로 짚어볼 수 있는 수준에서 포괄**하는 표현이어야 합니다.
  - 예시(X): "캐릭터 정보가 출력된다" (너무 모호 → 테스터가 어디를 봐야 할지 모름)
  - 예시(X, 반대 방향 실패): "월드맵 좌상단 난이도 변경 버튼에 현재 난이도(첫 진입 시 노말)가 표시되며, 터치 시 드롭다운으로 난이도 목록이 출력된다. 하드모드 미해금 시 자물쇠/비활성화 처리 및 해금조건(일반 4-8 클리어) 말풍선이 출력되고, 해금 후 하드 버튼 터치 시 하드모드 월드맵으로 전환된다." (4가지 기능을 한 줄에 욱여넣어 케이스가 비대해짐)
  - 예시(O): "월드맵 좌상단 난이도 변경 버튼이 출력되며, 난이도 변경이 가능하다" + "하드모드 해금 시(일반 4-8 클리어), 하드모드가 활성화된다" (버튼·해금 조건 단위로 시선을 집중시킨 형태)
- 통합으로 인해 어떤 기능이라도 검증 경로에서 빠지면 그 통합은 잘못된 것입니다. 통합 전후 기능 목록을 비교해 **누락이 없는지 반드시 확인**하세요.

## 2. 압축 기준 — 케이스 볼륨 감소
- 같은 화면·같은 흐름·같은 카테고리에서 **개별 요소를 확인하는 케이스들**은 한 줄로 통합:
  - "스킬 1 발동" / "스킬 2 발동" / ... / "스킬 N 발동" → "모든 스킬이 정상 발동되고 효과가 적용된다"
  - "버튼 A 클릭 시 동작" / "버튼 B 클릭 시 동작" / ... → "메인 메뉴의 모든 버튼이 정상 동작한다 (A·B·C·...)"
- **단계별 세부 절차**(누르고 → 확인 → 다시 누르고 → 확인)는 결과 중심 한 줄로 압축:
  - "메인 화면 진입 → 상점 버튼 클릭 → 상품 목록 표시 확인 → 구매 버튼 클릭 → 결제 팝업 표시" → "상점에서 상품 목록 확인 후 구매 플로우가 정상 동작한다"
- **한 케이스 안에 여러 기능을 욱여넣지 말 것 (중요).**
  - 표시 / 상호작용 / 조건부 동작 / 화면 전환 / 상태 전환 같은 **별개 기능**이 한 문장에 동시에 들어가면 케이스가 비대해져 체크리스트의 의미가 사라집니다.
  - 기준: 한 문장에 "~며, ~고, ~되며, ~하면" 등으로 **3개 이상의 동작/조건이 연결**되면 분리하세요.
  - 분리 방향: **버튼·화면·해금 조건·동작 단위(출력/전환/표시)** 같이 테스터가 육안으로 짚을 수 있는 단위로 쪼개고, 각 단위는 1~2개의 동작만 담는 짧은 문장으로 만듭니다.
  - 분리 예시 1 (월드맵 난이도):
    - Before(X): "월드맵 좌상단 난이도 변경 버튼에 현재 난이도가 표시되며, 터치 시 드롭다운으로 난이도 목록이 출력된다. 하드모드 미해금 시 자물쇠/비활성화 처리 및 해금조건 말풍선이 출력되고, 해금 후 하드 버튼 터치 시 하드모드 월드맵으로 전환된다."
    - After(O):
      - "월드맵 좌상단 난이도 변경 버튼이 출력되며, 난이도 변경이 가능하다"
      - "하드모드 해금 시(일반 4-8 클리어), 하드모드가 활성화된다"
  - 분리 예시 2 (누적 보상 UI):
    - Before(X): "화면 좌측 하단에 누적 뽑기 보상 UI가 출력되며, 안내 문구·누적 게이지가 표시된다. 뽑기 진행에 따라 게이지가 증가하고, 보상 수령 가능 시 UI가 녹색으로 전환되며 알림점이 출력된다. [전체확인] 버튼 터치 시 뽑기 단계별 보상 UI가 팝업된다."
    - After(O):
      - "화면 좌측 하단에 누적 뽑기 보상 UI(안내 문구·누적 게이지)가 출력된다"
      - "[전체확인] 버튼 터치 시 뽑기 단계별 보상 UI가 팝업된다"
      - (게이지 색 전환/알림점 출력 같은 상태 전환 디테일은 제외 — 아래 "저우선순위 요소" 참고)
- **유사한 엣지 케이스 다수**는 대표 1~2개만 남기되 expectationResult에 "(잘못된 입력 / 네트워크 단절 등 비정상 상황 포함)"으로 범위 명시.
- **첨부된 이미지·하이퍼링크·외부 참조 링크 등 보조 자료는 모두 삭제 가능합니다.** 체크리스트는 ���른 검증이 목적이라 본문 텍스트만으로 충분합니다.
  - 예시: "기획서 링크: https://... 참고" → 통째로 제거
  - 예시: "[이미지: spec_v3.png 참고]" → 통째로 제거
  - 단, expectationResult 본문에서 의미가 그 링크/이미지에만 의존하던 경우엔 핵심 내용을 텍스트로 풀어서 남기세요.

## 3. 체크리스트에서 의도적으로 제외하거나 압축하는 저우선순위 요소
빠른 1회 검증이 목적인 체크리스트에서는 다음 항목들의 검증 우선순위가 낮습니다. 별도 행으로 두지 말고, 다른 케이스에 흡수시키거나 통째로 생략하세요.
- **알림점 / 빨간 느낌표 / 뱃지**: "새 상품 알림점", "신규 모집 알림점" 등은 별도 행으로 두지 마세요. 본문에서도 가능하면 제거합니다.
- **info / 도움말 / 안내문구 버튼의 보조 팝업**: "info 버튼 터치 시 안내 팝업이 출력된다" 같은 행은 기본적으로 제거. 문제가 발생해도 유저 영향이 작은 영역입니다.
- **상세 확률표 / 등급별·완제별·영혼석별 등장 확률 목록**: "상세 확률 팝업 진입" 1건만 남기고, 팝업 내부의 등급별/완제별/영혼석별 확률 목록 검증 행은 제거합니다. 숙련 QA가 팝업을 열어 육안으로 확인합니다.
- **상태 전환 디테일**: 게이지 색 변경(일반→녹색→빨강/Complete), 단계 활성/비활성 전환, 미해금/해금 분기, 안내문구 텍스트 변화 같은 항목은 핵심 동작과 같은 행에 두지 말고, 가능하면 행 자체를 생략합니다. 1개 케이스에 너무 많은 상태를 묶으면 테스트 시간이 길어집니다.
- **연출 / 애니메이션 / SFX**: 단독 행으로 두지 말고, 결과 화면 검증에 흡수시키거나 "연출이 재생된다" 수준의 통합 한 줄로 처리합니다.

## 4. 반복되는 유사 섹션 처리 (중요한 가드)
- 한 게임 안에 "쿠키 뽑기 / 아티팩트 뽑기 / 레전더리 뽑기 / 스킨 뽑기"처럼 **거의 동일한 플로우가 여러 섹션에 반복**되는 경우가 있습니다.
- 반복 섹션 전체를 "기타 ○○ 기능은 다른 ○○와 동일" 식의 단일 행으로 통째로 축약하는 것은 **AI가 임의로 수행하지 마세요.** 어느 섹션을 어디까지 합칠지는 TC를 검토하는 QA 엔지니어가 육안으로 판단할 영역입니다.
- AI는 각 반복 섹션에 대해 동일한 원칙(2~3번 규칙)을 일관되게 적용한 압축 결과를 그대로 출력합니다. 일부 섹션만 통째로 한 줄로 사라지거나, 일부 섹션만 자세히 남는 비대칭은 피하세요.
- 단, 같은 섹션 안에서의 통합(2번 규칙)과 저우선순위 요소 제거(3번 규칙)는 정상적으로 적용합니다.

## 5. 통합 후 목표 볼륨
- 기존 TC 100건 기준으로 **20~40건 수준**까지 축소를 목표로 합니다. (정확한 숫자는 콘텐츠 양에 따라 가감)
- TC 한 건당 검증 시간이 **30초~1분 내외**가 되도록 작성 (한 줄에 너무 많은 걸 합쳐 검증 5분 걸리면 안 됨).
- 너무 적게 줄이면(원본의 70% 이상 유지) 체크리스트화의 의미가 없으므로 적극적으로 통합하세요.
- 반대로 한 케이스가 너무 비대해져도(한 줄에 4~5개 기능) 체크리스트의 의미가 사라지므로 **간결한 여러 줄 > 비대한 한 줄** 원칙을 지키세요.

## 6. 구조 유지 (main / sub1 / sub2 / sub3)
- 분류 컬럼은 **main · sub1 · sub2 · sub3** 의 4단계 체계를 사용합니다.
  - **main**: 대분류 (예: "뽑기 메인화면", "레전더리 뽑기")
  - **sub1**: 중분류 (예: "뽑기 화면", "레전더리 쿠키 뽑기")
  - **sub2**: 소분류 — 같은 sub1 안에서의 그룹화 (예: "픽업 뽑기", "상시 뽑기", "탭 버튼")
  - **sub3**: 검증 동작/상태 키워드 — 필요 시에만 채움 (예: "출력", "전환", "표시", "공통")
- 가능한 한 기존 구조를 유지하되, 픽업/상시 같은 묶음 단위는 sub2로 그룹화하고, 같은 그룹 안에서 동작 단위로 행을 나눠야 할 때 sub3에 동작 키워드를 둡니다.
- sub2·sub3은 통합 과정에서 자연스럽게 비어있을 수 있습니다(강제로 채우지 말 것).
- TC 순서는 기존 흐름(UI 진입 순서)을 따라 정렬.

## 7. Priority 기준 (체크리스트용)
- 체크리스트는 빠른 1회 검증이 목적이므로 **Critical / Major 위주**로 단순화하세요.
  - Critical: 진입·이탈·핵심 기능·보상 지급 등 절대 빠지면 안 되는 검증
  - Major: 주요 UI / 상태 변경 / 조건부 동작
  - Normal: 효과음·애니메이션·연출 등 (체크리스트에서는 다른 케이스와 합쳐 한 줄로 통합 가능)
- 통합된 케이스의 Priority는 통합 전 가장 높았던 케이스의 Priority를 따릅니다.

## 8. 분류 (변경 계획서용)
- 이 작업의 모든 변환은 **"수정(modified)" 또는 "제거(removed)"** 두 분류로만 들어갑니다.
  - **수정**: 통합되어 새 한 줄로 합쳐진 케이스 (대표 no를 유지, 나머지는 제거됨)
  - **제거**: 통합 결과 한 줄로 합쳐진 후 더 이상 별도로 존재하지 않는 케이스 / 저우선순위(3번 규칙)로 인해 통째로 빠진 케이스
- **"추가"는 사실상 발생하지 않음**. 기획서가 없으므로 새 기능을 추가할 근거가 없습니다.
  - 단, 여러 케이스에 분산되어 있던 **공통 규칙(예: "천장 수치는 다른 픽업 뽑기와 공유된다")** 을 한 줄로 분리해 명시할 필요가 있는 경우, 예외적으로 "추가" 행 1건을 만들 수 있습니다.
  - 또한 기존 TC가 명백히 검증해야 했음에도 빠뜨린 항목을 발견한 경우에도 예외적으로 "추가" 허용.

## 9. no(번호) 처리
- 통합 후 대표 케이스는 **통합 전 그룹 중 가장 낮은 no를 유지**합니다.
- 통합되어 흡수된 나머지 케이스는 "제거" 분류로 들어갑니다.
- 최종 결과의 no는 빈 번호 없이 1번부터 순차로 재정리해도 됩니다 (시스템이 별도 지시).

## 10. 다중 시트 자료 (xlsx 업로드 시)
- 업로드된 xlsx의 시트가 여러 개인 경우, 각 TC의 main 앞에 `[시트명]` 형태로 prefix가 박혀 있을 수 있습니다.
- 이 prefix는 **그대로 유지**하세요. 통합·압축은 같은 prefix(같은 시트) 안에서만 수행합니다.
- 시트가 다른 케이스끼리는 합치지 않습니다 (사용자가 시트로 영역을 구분해둔 의도).

# 단계별 흐름 안내
이 프롬프트는 TC 수정 흐름의 두 단계에서 호출됩니다:
1. **변경 계획서 단계** — 어떤 TC를 어떻게 통합·압축할지 마크다운으로 분석/계획
   - 통합 그룹별로 "어느 케이스들을 → 어느 한 줄로 합칠지" + "기능 누락 점검" 을 명시
2. **TC 수정 실행 단계** — 계획서대로 최종 TC JSON 배열 출력

각 단계의 출력 형식은 시스템이 추가로 지시합니다. 그 지시를 정확히 따라주세요.

# 출력 요청
위 원칙에 따라 기존 TC를 빠른 검증용 체크리스트로 압축해주세요. **기능 누락 없이 케이스 볼륨을 크게 줄이되, 한 케이스가 비대해지지 않도록 버튼·화면·동작 단위로 간결하게 작성**하고, 알림점·info 팝업·상세 확률 목록·상태 전환 디테일 같은 저우선순위 요소는 적극적으로 제거하는 것이 목표입니다.